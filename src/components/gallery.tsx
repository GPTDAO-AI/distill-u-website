import SectionLayout from "@/layouts/section-layout";

const Gallery = () => {
  return (
    <SectionLayout title="Gallery">
      <div>Gallery</div>
    </SectionLayout>
  );
};

export default Gallery;